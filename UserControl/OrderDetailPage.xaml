<Page x:Class="login_store.OrderDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent"
      Title="OrderDetailPage"
      Loaded="Page_Loaded">

    <Border CornerRadius="12" ClipToBounds="True" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Background>
            <ImageBrush ImageSource="/login_store;component/Images/BG_SHOP/BG5.jpg" Stretch="Fill"/>
        </Border.Background>
        <Border CornerRadius="10" BorderThickness="2" Opacity="0.95">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="Black" Offset="0"/>
                    <GradientStop Color="Black" Offset="0.75"/>
                    <GradientStop Color="Black" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,15,0" Panel.ZIndex="2">
                    <Button x:Name="btnMinimize" Click="MinimizeButton_Click" Content="—" FontWeight="Bold" FontSize="14" Cursor="Hand" VerticalAlignment="Top">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="Gray"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" Width="30" Height="30" CornerRadius="4">
                                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#4C4C4C"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button x:Name="btnMaximizeRestore" Click="MaximizeRestoreButton_Click" Content="☐" FontWeight="Bold" FontSize="14" Cursor="Hand" VerticalAlignment="Top" Margin="5,0,0,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="Gray"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" Width="30" Height="30" CornerRadius="4">
                                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#4C4C4C"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Button x:Name="btnClose" Click="CloseButton_Click" Content="X" FontWeight="Bold" FontSize="14" Cursor="Hand" VerticalAlignment="Top" Margin="5,0,0,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="Gray"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" Width="30" Height="30" CornerRadius="4">
                                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#E81123"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10, 50, 10, 10">
                    <StackPanel Orientation="Vertical" Margin="20">

                        <TextBlock Text="รายละเอียดคำสั่งซื้อ" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Left" Margin="0,0,0,10"/>

                        <Border Background="#1AFFFFFF" CornerRadius="5" Padding="15" Margin="0,0,0,10">
                            <StackPanel>
                                <TextBlock x:Name="txtOrderId" Text="Order #..." Foreground="White" FontSize="18" FontWeight="Bold"/>
                                <TextBlock x:Name="txtOrderDate" Text="วันที่: ..." Foreground="LightGray" FontSize="14" Margin="0,5,0,0"/>
                                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                    <TextBlock Text="สถานะ:" Foreground="LightGray" FontSize="14"/>
                                    <TextBlock x:Name="txtOrderStatus" Text="..." Foreground="Orange" FontSize="14" FontWeight="Bold" Margin="5,0,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="รายการสินค้า" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,10,0,5"/>
                        <Border Background="#1AFFFFFF" CornerRadius="5" Padding="10">
                            <StackPanel x:Name="ItemsPanel" Orientation="Vertical">
                            </StackPanel>
                        </Border>

                        <TextBlock x:Name="txtTotalPrice" Text="ยอดรวมทั้งหมด: 0.00 บาท" Foreground="LightGreen" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,15,0,15"/>

                        <Button x:Name="btnDownloadBill" Content="ดาวน์โหลดบิล (PDF)"
                                 Click="BtnDownloadBill_Click"
                                 Background="DodgerBlue" Foreground="White"
                                 FontSize="16" FontWeight="Bold" Padding="10" Margin="0,10,0,10"
                                 HorizontalAlignment="Left"/>
                        


                        <TextBlock Text="← กลับไปที่ประวัติการสั่งซื้อ"
                                    Foreground="White" FontSize="12" Cursor="Hand"
                                    MouseLeftButtonUp="BackToOrders_Click"
                                    HorizontalAlignment="Left" Margin="0,10,0,0"/>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Border>
</Page>