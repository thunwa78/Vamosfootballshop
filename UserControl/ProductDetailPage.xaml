<Page x:Class="login_store.ProductDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="Transparent" 
      Loaded="Page_Loaded"
      Foreground="White">

    <Page.Resources>
        <Style x:Key="ProActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F39C12"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E67E22"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#555"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FavoriteToggleStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#E81123"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Foreground" Value="#E81123"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Content" Value="♡"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="10">
                            <TextBlock x:Name="content" Text="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123"/>
                                <Setter TargetName="content" Property="Text" Value="♥"/>
                                <Setter TargetName="content" Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="QuantityStepperButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="#EEE"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#DDD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4C4C4C"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="WindowCloseButton" BasedOn="{StaticResource WindowControlButton}" TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Border CornerRadius="12" ClipToBounds="True" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Background>
            <ImageBrush ImageSource="/login_store;component/Images/BG_SHOP/BG5.jpg" Stretch="Fill"/>
        </Border.Background>

        <Border CornerRadius="10" BorderThickness="2" Opacity="0.95" Background="#111111">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="Black" Offset="0"/>
                    <GradientStop Color="Black" Offset="0.75"/>
                    <GradientStop Color="Black" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>

            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,15,0" Panel.ZIndex="2">
                    <Button x:Name="btnMinimize" Click="MinimizeButton_Click" Content="—" FontWeight="Bold" FontSize="14" Style="{StaticResource WindowControlButton}"/>
                    <Button x:Name="btnMaximizeRestore" Click="MaximizeRestoreButton_Click" Content="☐" FontWeight="Bold" FontSize="14" Style="{StaticResource WindowControlButton}" Margin="5,0,0,0"/>
                    <Button x:Name="btnClose" Click="CloseButton_Click" Content="X" FontWeight="Bold" FontSize="14" Style="{StaticResource WindowCloseButton}" Margin="5,0,0,0"/>
                </StackPanel>

                <Button Content="← กลับไปที่ร้านค้า" 
                        HorizontalAlignment="Left" VerticalAlignment="Top" 
                        Margin="20,20,0,0" Panel.ZIndex="1"
                        Background="Transparent" Foreground="White" 
                        Cursor="Hand" BorderThickness="0"
                        Click="BackToShop_Click"/>

                <Grid Margin="40,60,40,40">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Margin="0,0,20,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Grid.Row="0" BorderBrush="#888" BorderThickness="1" Margin="0,0,0,10" Background="White" CornerRadius="8">
                            <Viewbox Stretch="Uniform">
                                <Image x:Name="imgMainProduct" Width="400" Height="400"
                                       Stretch="Uniform" Cursor="Hand" MouseLeftButtonUp="ImageZoom_Click"/>
                            </Viewbox>
                        </Border>
                        <ListBox x:Name="thumbnailListBox" Grid.Row="1" Height="60"
                                 SelectionChanged="ThumbnailListBox_SelectionChanged"
                                 Background="Transparent" BorderThickness="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                 ScrollViewer.VerticalScrollBarVisibility="Disabled">
                            <ListBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ListBox.ItemsPanel>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="Gray" BorderThickness="2" Width="50" Height="50" Margin="0,0,5,0" CornerRadius="4" Cursor="Hand">
                                        <Image Source="{Binding ImagePreview}"/>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <ContentPresenter/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Border.BorderBrush" Value="DodgerBlue"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </Grid>

                    <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="10,0,0,0">
                            <TextBlock x:Name="txtBrandCategory" Text="Brand / Category" Foreground="#AAA" FontSize="14" Margin="0,0,0,5"/>
                            <TextBlock x:Name="txtName" Text="Product Name" Foreground="White" FontSize="36" FontWeight="Bold" TextWrapping="Wrap" LineHeight="42"/>
                            <TextBlock x:Name="txtPrice" Text="0.00 บาท" Foreground="#2ECC71" FontSize="32" FontWeight="Bold" Margin="0,10,0,15"/>
                            <TextBlock Text="รายละเอียดสินค้า" Foreground="White" FontSize="16" FontWeight="Bold" Margin="0,10,0,5"/>
                            <TextBlock x:Name="txtDescription" Text="Description..." Foreground="#DDD" TextWrapping="Wrap" Margin="0,0,0,15" LineHeight="20"/>
                            <Separator Background="#333" Margin="0,10"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="เลือกขนาด (Size):" Foreground="White" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                                <Button x:Name="btnSizeChart" Grid.Column="1" Content="ตารางไซส์ 📏" 
                                        Background="Transparent" Foreground="#58A6FF" Cursor="Hand"
                                        Click="BtnSizeChart_Click"/>
                            </Grid>

                            <ListBox x:Name="sizeListBox" 
                                     Background="Transparent" BorderThickness="0"
                                     SelectionChanged="SizeListBox_SelectionChanged"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>

                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="Background" Value="White"/>
                                        <Setter Property="BorderBrush" Value="#AAA"/>
                                        <Setter Property="BorderThickness" Value="1"/>
                                        <Setter Property="Foreground" Value="Black"/>
                                        <Setter Property="Padding" Value="15,8"/>
                                        <Setter Property="Margin" Value="0,0,5,5"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="Content" Value="{Binding SizeName}"/>
                                        <Setter Property="IsEnabled" Value="{Binding IsInStock}"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                                    </Border>

                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter TargetName="border" Property="Background" Value="Black"/>
                                                            <Setter TargetName="border" Property="BorderBrush" Value="Black"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>

                                                        <Trigger Property="IsEnabled" Value="False">
                                                            <Setter TargetName="border" Property="Background" Value="#EEE"/>
                                                            <Setter TargetName="border" Property="BorderBrush" Value="#DDD"/>
                                                            <Setter Property="Foreground" Value="#AAA"/>
                                                            <Setter Property="TextBlock.TextDecorations" Value="Strikethrough"/>
                                                        </Trigger>

                                                        <MultiTrigger>
                                                            <MultiTrigger.Conditions>
                                                                <Condition Property="IsMouseOver" Value="True"/>
                                                                <Condition Property="IsSelected" Value="False"/>
                                                            </MultiTrigger.Conditions>
                                                            <Setter TargetName="border" Property="BorderBrush" Value="Black"/>
                                                        </MultiTrigger>

                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                            </ListBox>

                            <TextBlock x:Name="txtStock" Text="กรุณาเลือกขนาด" Foreground="Red" FontSize="14" Margin="0,5,0,10"/>
                            <TextBlock Text="จำนวน:" Foreground="White" FontSize="16" FontWeight="Bold" Margin="0,10,0,5"/>

                            <Grid Width="150" HorizontalAlignment="Left">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0" Content="-" 
                                        Style="{StaticResource QuantityStepperButtonStyle}" 
                                        Click="btnDecreaseQuantity_Click"/>
                                <TextBox Grid.Column="1" x:Name="txtQuantity" Text="1" 
                                         VerticalContentAlignment="Center" HorizontalContentAlignment="Center" 
                                         FontSize="16" FontWeight="Bold" 
                                         Margin="5,0" BorderThickness="0"
                                         PreviewTextInput="Quantity_PreviewTextInput"/>
                                <Button Grid.Column="2" Content="+" 
                                        Style="{StaticResource QuantityStepperButtonStyle}" 
                                        Click="btnIncreaseQuantity_Click"/>
                            </Grid>

                            <StackPanel Orientation="Horizontal" Margin="0,30,0,0">
                                <Button x:Name="btnAddToCart" Content="🛒 เพิ่มลงตะกร้า"
                                        Style="{StaticResource ProActionButtonStyle}"
                                        MinWidth="250"
                                        Click="BtnAddToCart_Click"/>
                                <ToggleButton x:Name="btnToggleFavorite" 
                                              Style="{StaticResource FavoriteToggleStyle}"
                                              Margin="10,0,0,0"
                                              Click="BtnToggleFavorite_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Border>
    </Border>
</Page>