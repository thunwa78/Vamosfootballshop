<Window x:Class="login_store.AddEditProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:login_store"
        mc:Ignorable="d"
        Title="AddEditProductWindow" Height="700" Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize"
        Loaded="Window_Loaded">

    <Border Background="#1C1C1E" CornerRadius="12" BorderBrush="#333" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                <TextBlock x:Name="txtTitle" Text="เพิ่ม/แก้ไขสินค้า" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0"/>
                <Button x:Name="btnClose" Click="btnClose_Click" Content="X" FontWeight="Bold" Width="30" Height="30" Background="DarkRed" Foreground="White" BorderThickness="0"/>
            </StackPanel>

            <TabControl Grid.Row="1" Margin="10" Background="#2C2C2E">

                <TabItem Header="ข้อมูลหลัก" Background="#3A3A3C" Foreground="White">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="5">
                        <StackPanel Margin="15">
                            <TextBlock Text="ชื่อสินค้า:" Foreground="White"/>
                            <TextBox x:Name="txtName" Padding="5"/>

                            <TextBlock Text="คำอธิบาย:" Foreground="White" Margin="0,10,0,0"/>
                            <TextBox x:Name="txtDescription" Padding="5" Height="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="หมวดหมู่ (Category):" Foreground="White"/>
                                    <ComboBox x:Name="cmbCategory" DisplayMemberPath="Name" SelectedValuePath="CategoryId" Padding="5"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock Text="แบรนด์ (Brand):" Foreground="White"/>
                                    <ComboBox x:Name="cmbBrand" 
                                              DisplayMemberPath="Name" 
                                              SelectedValuePath="BrandId" 
                                              Padding="5"
                                              SelectionChanged="cmbBrand_SelectionChanged"/>
                                </StackPanel>
                            </Grid>

                            <StackPanel Margin="0,10,0,0">
                                <TextBlock Text="รุ่นสินค้า (Model):" Foreground="White"/>
                                <ComboBox x:Name="cmbModel" DisplayMemberPath="Name" SelectedValuePath="ModelId" Padding="5" IsEnabled="False"/>
                            </StackPanel>

                            <TextBlock Text="ราคา (บาท):" Foreground="White" Margin="0,10,0,0"/>
                            <TextBox x:Name="txtPrice" Padding="5" PreviewTextInput="NumberValidationTextBox"/>

                            <TextBlock Text="รูปภาพปก (Main Image Path):" Foreground="White" Margin="0,10,0,0"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" x:Name="txtImagePath" Padding="5" IsReadOnly="True" Background="#555"/>
                                <Button Grid.Column="1" x:Name="btnBrowseMainImage" Content="Browse..." Margin="5,0,0,0" Padding="10,5" Click="btnBrowseMainImage_Click"/>
                            </Grid>
                            <Image x:Name="imgMainPreview" Height="100" Margin="0,5,0,0" Stretch="Uniform"/>

                            <TextBlock Text="รูปภาพตารางไซส์ (Size Chart Path):" Foreground="White" Margin="0,15,0,0"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" x:Name="txtSizeChartPath" Padding="5" IsReadOnly="True" Background="#555"/>
                                <Button Grid.Column="1" x:Name="btnBrowseSizeChart" Content="Browse..." Margin="5,0,0,0" Padding="10,5" Click="btnBrowseSizeChart_Click"/>
                            </Grid>
                            <Image x:Name="imgSizeChartPreview" Height="100" Margin="0,5,0,0" Stretch="Uniform"/>
                            <CheckBox x:Name="chkIsActive" Content="แสดงสินค้านี้ (Active)" Foreground="White" Margin="0,15,0,0" IsChecked="True"/>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="ไซส์ และ สต็อก (Variants)" Background="#3A3A3C" Foreground="White">
                    <Grid Margin="15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="#3A3A3C" CornerRadius="5" Padding="10" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="1*"/>
                                    <ColumnDefinition Width="0.8*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Text="ชื่อไซส์ (เช่น S, M):" Foreground="White"/>
                                    <TextBox x:Name="txtVariantSize" Padding="5"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                    <TextBlock Text="สต็อก:" Foreground="White"/>
                                    <TextBox x:Name="txtVariantStock" Padding="5" PreviewTextInput="NumberValidationTextBox"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                                    <TextBlock Text="ลำดับ (1,2,3):" Foreground="White"/>
                                    <TextBox x:Name="txtVariantSort" Padding="5" PreviewTextInput="NumberValidationTextBox" Text="0"/>
                                </StackPanel>

                                <Button Grid.Column="3" x:Name="btnAddVariant" Content="เพิ่ม/อัปเดต" Padding="15,5" VerticalAlignment="Bottom" Click="btnAddVariant_Click"/>
                            </Grid>
                        </Border>

                        <DataGrid Grid.Row="1" x:Name="VariantsDataGrid" AutoGenerateColumns="False" CanUserAddRows="False"
                                  Background="#1AFFFFFF" Foreground="White" BorderBrush="#333"
                                  SelectionChanged="VariantsDataGrid_SelectionChanged">

                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="#3AFFFFFF"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2AFFFFFF"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#4C007ACC"/>
                                            <Setter Property="BorderBrush" Value="#007ACC"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="BorderBrush" Value="#222"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding VariantId}" IsReadOnly="True" Width="40"/>
                                <DataGridTextColumn Header="ลำดับ" Binding="{Binding SortOrder}" Width="60"/>
                                <DataGridTextColumn Header="ชื่อไซส์" Binding="{Binding SizeName}" Width="*"/>
                                <DataGridTextColumn Header="สต็อก" Binding="{Binding StockQuantity}" Width="80"/>
                                <DataGridTemplateColumn Header="จัดการ" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="ลบ" Background="DarkRed" Foreground="White" Tag="{Binding VariantId}" Click="btnDeleteVariant_Click"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>

                <TabItem Header="แกลเลอรีรูปภาพ" Background="#3A3A3C" Foreground="White">
                    <Grid Margin="15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="145*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Button Grid.Row="0" x:Name="btnAddGalleryImage" Content="เพิ่มรูปภาพแกลเลอรี..." Padding="10" HorizontalAlignment="Left" Click="btnAddGalleryImage_Click" Grid.ColumnSpan="2" Margin="0,0,0,462" Grid.RowSpan="2"/>

                        <ListBox Grid.Row="1" x:Name="GalleryImagesListBox" Margin="0,10,0,0" Background="#2C2C2E" BorderBrush="#555" Grid.ColumnSpan="2">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#3A3A3C" CornerRadius="5" Padding="5" Margin="5" Width="500">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Image Source="{Binding ImagePreview}" Height="80" Stretch="Uniform"/>
                                            <TextBlock Grid.Column="1" Text="{Binding ImagePath}" VerticalAlignment="Center" Margin="10,0" Foreground="LightGray" TextTrimming="CharacterEllipsis"/>
                                            <Button Grid.Column="2" Content="ลบ" Background="DarkRed" Foreground="White" Tag="{Binding ImageId}" Click="btnDeleteGalleryImage_Click"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </TabItem>

            </TabControl>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                <Button x:Name="btnSave" Content="บันทึกข้อมูล" Width="120" Padding="10" Background="DarkGreen" Foreground="White" Margin="5" Click="btnSave_Click"/>
                <Button x:Name="btnCancel" Content="ยกเลิก" Width="100" Padding="10" Background="#555" Foreground="White" Margin="5" Click="btnClose_Click"/>
            </StackPanel>

        </Grid>
    </Border>
</Window>