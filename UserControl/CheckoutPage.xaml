<Page x:Class="login_store.CheckoutPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d"
      d:DesignHeight="700" d:DesignWidth="800"
      Background="Transparent"
      Title="CheckoutPage" Loaded="Page_Loaded">

    <Page.Resources>
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="WindowCloseButton" BasedOn="{StaticResource WindowControlButton}" TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                </Trigger>
            </Style.Triggers>
        </Style>
         <Style x:Key="ProActionButtonGreenStyle" TargetType="Button">
            <Setter Property="Background" Value="#2ECC71"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#ADADAD"/>
            <Setter Property="BorderBrush" Value="#ADADAD"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Border CornerRadius="12" ClipToBounds="True" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Background>
            <ImageBrush ImageSource="/login_store;component/Images/BG_SHOP/BG5.jpg" Stretch="Fill"/>
        </Border.Background>

        <Border CornerRadius="10" BorderThickness="2" Opacity="0.95" Background="#111111" Margin="10">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="Black" Offset="0"/><GradientStop Color="Black" Offset="0.75"/><GradientStop Color="Black" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Grid>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,15,0" Panel.ZIndex="3">
                    <Button x:Name="btnMinimize" Click="MinimizeButton_Click" Content="—" FontWeight="Bold" FontSize="14" Style="{StaticResource WindowControlButton}"/>
                    <Button x:Name="btnMaximizeRestore" Click="MaximizeRestoreButton_Click" Content="☐" FontWeight="Bold" FontSize="14" Style="{StaticResource WindowControlButton}" Margin="5,0,0,0"/>
                    <Button x:Name="btnClose" Click="CloseButton_Click" Content="X" FontWeight="Bold" FontSize="14" Style="{StaticResource WindowCloseButton}" Margin="5,0,0,0"/>
                </StackPanel>

                <Grid Margin="10, 50, 10, 10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" BorderBrush="#333" BorderThickness="0,0,0,1" Margin="10,0,10,10" Padding="0,0,0,10">
                        <Button x:Name="btnBackToConfirmation" Content="← กลับไปหน้ายืนยัน" 
                                HorizontalAlignment="Left" VerticalAlignment="Center" 
                                Background="Transparent" Foreground="White" 
                                FontSize="14" Cursor="Hand" Click="btnBackToConfirmation_Click"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="450">
                            <StackPanel>
                                <TextBlock Text="ขั้นตอนสุดท้าย: ชำระเงิน" Foreground="White" FontSize="30" FontWeight="Bold" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                                <TextBlock Text="กรุณาโอนเงินมาที่บัญชี:" Foreground="#AAA" FontSize="14" HorizontalAlignment="Center"/>
                                
                                <Border Background="#222" CornerRadius="5" Padding="15" Margin="0,10,0,0">
                                    <StackPanel HorizontalAlignment="Center">
                                        
                                        <Image x:Name="qrCodeImage" Width="200" Height="200" Margin="10"/>
                                        
                                        <TextBlock Text="VAMOS SHOP" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        <TextBlock Text="ธนาคารออมสิน" Foreground="#AAA" HorizontalAlignment="Center"/>
                                        <TextBlock Text="020-388-76-3540" Foreground="#AAA" HorizontalAlignment="Center" FontSize="16" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </Border>

                                <TextBlock Text="ยอดที่ต้องชำระ:" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,20,0,5" HorizontalAlignment="Center"/>
                                
                                <TextBlock x:Name="txtAmountToPay" Text="0.00 บาท" Foreground="LightGreen" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center"/>
                                
                                <Separator Background="#333" Margin="0,20"/>
                                
                                <TextBlock Text="อัปโหลดสลิปโอนเงิน:" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                <Border BorderBrush="#666" BorderThickness="2"  Height="150" CornerRadius="4" Background="#333" Margin="0,0,0,10">
                                    <Grid>
                                        <Image x:Name="imgSlipPreview" Stretch="Uniform"/>
                                        <TextBlock x:Name="txtSlipPlaceholder" Text="(ยังไม่ได้เลือกรูปภาพ)" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#AAA"/>
                                    </Grid>
                                </Border>
                                
                                <Button Content="เลือกไฟล์สลิป..." Click="btnBrowseSlip_Click" 
                                        Style="{StaticResource SecondaryActionButtonStyle}" 
                                        Padding="10,5" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                                <Button x:Name="btnConfirmOrder" Content="ยืนยันการสั่งซื้อ" 
                                        Style="{StaticResource ProActionButtonGreenStyle}"
                                        Click="btnConfirmOrder_Click"
                                        Margin="0,10,0,0"/>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Border>
    </Border>
</Page>