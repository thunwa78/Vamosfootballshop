<Window x:Class="login_store.AddEditVoucherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add/Edit Voucher" Height="550" Width="450"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded"
        MouseLeftButtonDown="Border_MouseLeftButtonDown">

    <Border Background="#1C1C1E" CornerRadius="12" BorderBrush="#333" BorderThickness="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                <TextBlock x:Name="txtTitle" Text="สร้างโค้ดส่วนลดใหม่" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0"/>
                <Button x:Name="btnClose" Click="btnClose_Click" Content="X" FontWeight="Bold" Width="30" Height="30" Background="DarkRed" Foreground="White" BorderThickness="0"/>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="15,10">
                <StackPanel Orientation="Vertical">

                    <TextBlock Text="โค้ด (เช่น VAMOS100)" Foreground="White" Margin="0,5,0,5"/>
                    <TextBox x:Name="txtCode" Padding="5" CharacterCasing="Upper"/>

                    <TextBlock Text="คำอธิบาย (แสดงให้ User เห็น)" Foreground="White" Margin="0,10,0,5"/>
                    <TextBox x:Name="txtDescription" Padding="5"/>

                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Text="ประเภทส่วนลด" Foreground="White" Margin="0,0,0,5"/>
                            <ComboBox x:Name="cmbDiscountType" Padding="5" SelectedIndex="0" Margin="0,0,0,10">
                                <ComboBoxItem Content="จำนวนเงิน (บาท)"/>
                                <ComboBoxItem Content="เปอร์เซ็นต์ (%)"/>
                            </ComboBox>

                            <TextBlock Text="มูลค่าส่วนลด" Foreground="White" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtDiscountAmount" Padding="5" PreviewTextInput="NumberValidationTextBox"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="5,0,0,0" VerticalAlignment="Bottom">
                            <TextBlock Text="ยอดซื้อขั้นต่ำ (บาท)" Foreground="White" Margin="0,0,0,5"/>
                            <TextBox x:Name="txtMinPurchase" Padding="5" Text="0" PreviewTextInput="NumberValidationTextBox"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Text="วันที่เริ่มใช้" Foreground="White" Margin="0,0,0,5"/>
                            <DatePicker x:Name="dpValidFrom"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                            <TextBlock Text="วันที่หมดอายุ" Foreground="White" Margin="0,0,0,5"/>
                            <DatePicker x:Name="dpValidTo"/>
                        </StackPanel>
                    </Grid>

                    <CheckBox x:Name="chkIsActive" Content="เปิดใช้งานโค้ดนี้ทันที" Foreground="White" Margin="0,20,0,10" IsChecked="True"/>

                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="15">
                <Button x:Name="btnSave" Content="บันทึก" Width="100" Padding="10" Background="DarkGreen" Foreground="White" Margin="5" Click="btnSave_Click"/>
                <Button x:Name="btnCancel" Content="ยกเลิก" Width="100" Padding="10" Background="#555" Foreground="White" Margin="5" Click="btnClose_Click"/>
            </StackPanel>

        </Grid>
    </Border>
</Window>