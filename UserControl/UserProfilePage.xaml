<Page x:Class="login_store.UserProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:login_store"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="800"
      Background="Transparent"
      Title="UserProfilePage" Loaded="Page_Loaded">

    <Page.Resources>
        <Style x:Key="ProActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F39C12"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#ADADAD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#ADADAD"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="WindowCloseButton" BasedOn="{StaticResource WindowControlButton}" TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ProfileTabItemStyle" TargetType="TabItem">
            <Setter Property="Background" Value="#3A3A3C" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="White" />
                    <Setter Property="Foreground" Value="#111111" />
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="True"/>
                        <Condition Property="IsSelected" Value="False"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="Background" Value="#4A4A4C"/>
                </MultiTrigger>
            </Style.Triggers>
        </Style>

    </Page.Resources>

    <Border CornerRadius="12" ClipToBounds="True" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Background>
            <ImageBrush ImageSource="/login_store;component/Images/BG_SHOP/BG5.jpg" Stretch="Fill"/>
        </Border.Background>

        <Border CornerRadius="10" BorderThickness="2" Opacity="0.95" Background="#111111">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="Black" Offset="0"/>
                    <GradientStop Color="Black" Offset="0.75"/>
                    <GradientStop Color="Black" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,15,0" Panel.ZIndex="3">
                    <Button x:Name="btnMinimize" Click="MinimizeButton_Click" Content="—" FontWeight="Bold" FontSize="14" Style="{StaticResource WindowControlButton}"/>
                    <Button x:Name="btnMaximizeRestore" Click="MaximizeRestoreButton_Click" Content="☐" FontWeight="Bold" FontSize="14" Style="{StaticResource WindowControlButton}" Margin="5,0,0,0"/>
                    <Button x:Name="btnClose" Click="CloseButton_Click" Content="X" FontWeight="Bold" FontSize="14" Style="{StaticResource WindowCloseButton}" Margin="5,0,0,0"/>
                </StackPanel>

                <Grid Margin="10, 50, 10, 10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" BorderBrush="#333" BorderThickness="0,0,0,1" Margin="10,0,10,10" Padding="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Image Source="/login_store;component/Images/Vamoslogo1.png" Width="60" Height="60" Grid.Column="0" VerticalAlignment="Center" Cursor="Hand" MouseLeftButtonUp="Logo_Click"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="20,0,0,0">

                                <Button Content="ออกจากระบบ" Margin="10,0" Foreground="#E81123" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="Logout_Click"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button x:Name="btnVouchers" Content="🎟️" ToolTip="โค้ดส่วนลดของฉัน" Foreground="White" Margin="5,0" FontSize="16" Background="Transparent" BorderThickness="0"  Cursor="Hand" Click="btnVouchers_Click"/>
                                <Button x:Name="btnNotifications" Content="🔔" ToolTip="การแจ้งเตือน" Foreground="White" Margin="5,0" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnNotifications_Click"/>
                                <Button x:Name="btnOrders" Content="📦" ToolTip="ประวัติการสั่งซื้อ" Foreground="White" Margin="5" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnOrders_Click"/>
                                <Button x:Name="btnWishlist" Content="❤️" ToolTip="รายการโปรด" Foreground="White" Margin="5" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnWishlist_Click"/>
                                <Button x:Name="btnCart" Content="🛒" ToolTip="ตะกร้าสินค้า" Foreground="White" Margin="5,0" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnCart_Click"/>
                                <Button x:Name="btnProfile" Content="👤" ToolTip="โปรไฟล์" Foreground="#F39C12" FontWeight="Bold" Margin="5,0" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                                <Button x:Name="btAboutUs" Content="ℹ️" ToolTip="เกี่ยวกับเรา" Margin="10,0" Foreground="White" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnAboutUs_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <TextBlock Grid.Row="1" Text="โปรไฟล์ของฉัน" Foreground="White" FontSize="24" FontWeight="Bold" Margin="10,0,0,10"/>

                    <TabControl Grid.Row="2" Margin="10" Background="#2C2C2E">

                        <TabItem Header="ข้อมูลส่วนตัว" Style="{StaticResource ProfileTabItemStyle}">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel Margin="20">

                                    <Border HorizontalAlignment="Center" Width="120" Height="120" CornerRadius="60" BorderBrush="#555" BorderThickness="2" Margin="0,0,0,10">
                                        <Border.Background>
                                            <ImageBrush x:Name="ProfileImage" ImageSource="/Images/default_profile.png"/>
                                        </Border.Background>
                                        <Ellipse x:Name="ProfileEllipse" Fill="Gray"/>
                                    </Border>
                                    <Button Content="เปลี่ยนรูปโปรไฟล์" HorizontalAlignment="Center" Click="ChangeProfileImage_Click" Style="{StaticResource SecondaryActionButtonStyle}" Margin="0,0,0,20"/>

                                    <TextBlock Text="ชื่อผู้ใช้งาน (Username):" Foreground="White" Margin="0,10,0,5"/>
                                    <TextBox x:Name="txtUsername" IsReadOnly="True" Background="#555" Padding="5"/>

                                    <TextBlock Text="อีเมล:" Foreground="White" Margin="0,10,0,5"/>
                                    <TextBox x:Name="txtEmail" IsReadOnly="True" Background="#555" Padding="5"/>

                                    <TextBlock Text="ชื่อจริง:" Foreground="White" Margin="0,10,0,5"/>
                                    <TextBox x:Name="txtFirstName" Padding="5"/>

                                    <TextBlock Text="นามสกุล:" Foreground="White" Margin="0,10,0,5"/>
                                    <TextBox x:Name="txtLastName" Padding="5"/>

                                    <TextBlock Text="เบอร์โทรศัพท์:" Foreground="White" Margin="0,10,0,5"/>
                                    <TextBox x:Name="txtPhoneNumber" Padding="5"/>

                                    <Button x:Name="SaveChangesButton" Content="บันทึกข้อมูลส่วนตัว" 
                                            Style="{StaticResource ProActionButtonStyle}" 
                                            Click="SaveChanges_Click" 
                                            Margin="0,30,0,0" HorizontalAlignment="Left"/>
                                </StackPanel>
                            </ScrollViewer>
                        </TabItem>

                        <TabItem Header="ที่อยู่ของฉัน" Style="{StaticResource ProfileTabItemStyle}">
                            <Grid Margin="15">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Button x:Name="btnAddAddress" Grid.Row="0" Content="＋ เพิ่มที่อยู่ใหม่"
                                        Style="{StaticResource ProActionButtonStyle}" 
                                        Background="#2ECC71"
                                        Padding="15,8" HorizontalAlignment="Left" Margin="0,0,0,10"
                                        Click="btnAddAddress_Click"/>

                                <ListBox x:Name="AddressesListBox" Grid.Row="1" Background="#2C2C2E" BorderBrush="#555"
                                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#3A3A3C" CornerRadius="5" Padding="15" Margin="5" Width="500">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                                                        <TextBlock Text="{Binding AddressLabel}" FontSize="16" FontWeight="Bold" Foreground="White"/>
                                                        <TextBlock Text="(ที่อยู่หลัก)" Foreground="LightGreen" FontWeight="Bold" Margin="10,0,0,0">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsDefault}" Value="True">
                                                                            <Setter Property="Visibility" Value="Visible"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </StackPanel>

                                                    <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="0,5,0,0">
                                                        <TextBlock Text="{Binding FullName}" Foreground="#DDD"/>
                                                        <TextBlock Text=" | " Foreground="#555" Margin="5,0"/>
                                                        <TextBlock Text="{Binding PhoneNumber}" Foreground="#DDD"/>
                                                    </StackPanel>

                                                    <TextBlock Grid.Row="2" Grid.Column="0" TextWrapping="Wrap" Foreground="#AAA" Margin="0,5,0,0">
                                                        <Run Text="{Binding AddressLine1}"/>,
                                                        <Run Text="{Binding SubDistrict}"/>,
                                                        <Run Text="{Binding District}"/>,
                                                        <Run Text="{Binding Province}"/>
                                                        <Run Text="{Binding PostalCode}"/>
                                                    </TextBlock>

                                                    <StackPanel Grid.Row="0" Grid.Column="1" Grid.RowSpan="3" Orientation="Vertical" VerticalAlignment="Center">
                                                        <Button Content="แก้ไข" Tag="{Binding}" Click="btnEditAddress_Click" Style="{StaticResource SecondaryActionButtonStyle}" Background="DodgerBlue" Foreground="White" Margin="5"/>
                                                        <Button Content="ลบ" Tag="{Binding AddressId}" Click="btnDeleteAddress_Click" Style="{StaticResource SecondaryActionButtonStyle}" Background="DarkRed" Foreground="White" Margin="5"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                        </TabItem>

                    </TabControl>
                </Grid>
            </Grid>
        </Border>
    </Border>
</Page>