<Page x:Class="login_store.AboutUsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="800"
      Background="Transparent"
      Title="AboutUsPage" Loaded="Page_Loaded">

    <Page.Resources>
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="WindowCloseButton" BasedOn="{StaticResource WindowControlButton}" TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Border CornerRadius="12" ClipToBounds="True" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Background>
            <ImageBrush ImageSource="/login_store;component/Images/BG_SHOP/BG5.jpg" Stretch="Fill"/>
        </Border.Background>

        <Border CornerRadius="10" BorderThickness="2" Opacity="0.95" Background="#111111">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="Black" Offset="0"/>
                    <GradientStop Color="Black" Offset="0.75"/>
                    <GradientStop Color="Black" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,15,0" Panel.ZIndex="3">
                    <Button x:Name="btnMinimize" Click="MinimizeButton_Click" Content="—" FontWeight="Bold" FontSize="14" Style="{StaticResource WindowControlButton}"/>
                    <Button x:Name="btnMaximizeRestore" Click="MaximizeRestoreButton_Click" Content="☐" FontWeight="Bold" FontSize="14" Style="{StaticResource WindowControlButton}" Margin="5,0,0,0"/>
                    <Button x:Name="btnClose" Click="CloseButton_Click" Content="X" FontWeight="Bold" FontSize="14" Style="{StaticResource WindowCloseButton}" Margin="5,0,0,0"/>
                </StackPanel>

                <Grid Margin="10, 50, 10, 10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" BorderBrush="#333" BorderThickness="0,0,0,1" Margin="10,0,10,10" Padding="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Image Source="/login_store;component/Images/Vamoslogo1.png" Width="60" Height="60" Grid.Column="0" VerticalAlignment="Center" Cursor="Hand" MouseLeftButtonUp="Logo_Click"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="20,0,0,0">
                                <Button Content="ร้านค้า" Margin="10,0" Foreground="White" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnShop_Click"/>
                                <Button Content="คำสั่งซื้อ" Margin="10,0" Foreground="White" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnOrders_Click"/>

                                <Button Content="เกี่ยวกับเรา" Margin="10,0" Foreground="#F39C12" FontWeight="Bold" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnAboutUs_Click"/>

                                <Button Content="ออกจากระบบ" Margin="10,0" Foreground="#E81123" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="Logout_Click"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button x:Name="btnVouchers" Content="🎟️" ToolTip="โค้ดส่วนลดของฉัน" Foreground="White" Margin="5,0" FontSize="16" Background="Transparent" BorderThickness="0"  Cursor="Hand" Click="btnVouchers_Click"/>
                                <Button x:Name="btnNotifications" Content="🔔" ToolTip="การแจ้งเตือน" Foreground="White" Margin="5,0" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnNotifications_Click"/>
                                <Button x:Name="btnOrders" Content="📦" ToolTip="ประวัติการสั่งซื้อ" Foreground="White" Margin="5,0" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnOrders_Click"/>
                                <Button x:Name="btnWishlist" Content="❤️" ToolTip="รายการโปรด" Foreground="White" Margin="5" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnWishlist_Click"/>
                                <Button x:Name="btnCart" Content="🛒" ToolTip="ตะกร้าสินค้า" Foreground="White" Margin="5,0" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnCart_Click"/>
                                <Button x:Name="btnProfile" Content="👤" ToolTip="โปรไฟล์" Foreground="White" Margin="5,0" FontSize="16" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="btnProfile_Click"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="30">
                            <TextBlock Text="เกี่ยวกับ VAMOS Shop" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,0,0,20"/>

                            <TextBlock x:Name="txtDescription" Text="กำลังโหลด..." TextWrapping="Wrap" Foreground="#DDD" FontSize="16" LineHeight="24"/>

                            <TextBlock Text="ทีมงานของเรา" Foreground="White" FontSize="24" FontWeight="Bold" Margin="0,30,0,15"/>
                            <ListBox x:Name="TeamListBox" Background="Transparent" BorderThickness="0"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Width="150" Margin="10" 
                                                    Cursor="Hand"
                                                    MouseLeftButtonUp="TeamMember_Click"
                                                    Tag="{Binding}">
                                            <Border Width="120" Height="120" CornerRadius="60" Background="#333">
                                                <Image Source="{Binding ImagePreview}" Stretch="UniformToFill"/>
                                            </Border>
                                            <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                            <TextBlock Text="{Binding Role}" Foreground="#AAA" FontSize="12" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>

                            <Separator Background="#333" Margin="0,30,0,30"/>

                            <TextBlock Text="ติดต่อเรา (Contact Us)" Foreground="White" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

                            <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Text="📧" FontSize="16" Margin="0,0,10,0"/>
                                <TextBlock Text="อีเมล:" Foreground="#AAA" Width="80"/>
                                <TextBlock x:Name="txtEmail" Text="..." Foreground="White" FontSize="14"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Text="📞" FontSize="16" Margin="0,0,10,0"/>
                                <TextBlock Text="โทรศัพท์:" Foreground="#AAA" Width="80"/>
                                <TextBlock x:Name="txtPhone" Text="..." Foreground="White" FontSize="14"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Text="🆔" FontSize="16" Margin="0,0,10,0"/>
                                <TextBlock Text="Tax ID:" Foreground="#AAA" Width="80"/>
                                <TextBlock x:Name="txtTaxId" Text="..." Foreground="White" FontSize="14"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                <TextBlock Text="📍" FontSize="16" Margin="0,0,10,0"/>
                                <TextBlock Text="ที่อยู่:" Foreground="#AAA" Width="80" VerticalAlignment="Top"/>
                                <TextBlock x:Name="txtAddress" Text="..." Foreground="White" FontSize="14" TextWrapping="Wrap"/>
                            </StackPanel>

                        </StackPanel>
                    </ScrollViewer>

                </Grid>
            </Grid>
        </Border>
    </Border>
</Page>