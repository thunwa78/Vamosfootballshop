<Page x:Class="login_store.AdminManageAboutUsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="800"
      Background="Transparent"
      Title="AdminManageAboutUsPage" Loaded="Page_Loaded">

    <Page.Resources>
        <Style x:Key="ProActionButtonGreenStyle" TargetType="Button">
            <Setter Property="Background" Value="#2ECC71"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#ADADAD"/>
            <Setter Property="BorderBrush" Value="#ADADAD"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ℹ️ จัดการหน้า 'เกี่ยวกับเรา' (About Us)" Foreground="White" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <Border Background="#1AFFFFFF" CornerRadius="5" Padding="20" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="ข้อมูลหลัก (Company Info)" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>

                        <TextBlock Text="คำอธิบาย (Description):" Foreground="#AAA" Margin="0,5,0,5"/>
                        <TextBox x:Name="txtDescription" Padding="5" Height="120" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

                        <TextBlock Text="อีเมลติดต่อ:" Foreground="#AAA" Margin="0,10,0,5"/>
                        <TextBox x:Name="txtEmail" Padding="5"/>

                        <TextBlock Text="เบอร์โทรศัพท์:" Foreground="#AAA" Margin="0,10,0,5"/>
                        <TextBox x:Name="txtPhone" Padding="5"/>

                        <TextBlock Text="เลขประจำตัวผู้เสียภาษี:" Foreground="#AAA" Margin="0,10,0,5"/>
                        <TextBox x:Name="txtTaxId" Padding="5"/>

                        <TextBlock Text="ที่อยู่:" Foreground="#AAA" Margin="0,10,0,5"/>
                        <TextBox x:Name="txtAddress" Padding="5" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

                        <Button x:Name="btnSaveCompanyInfo" Content="บันทึกข้อมูลหลัก"
                                Style="{StaticResource ProActionButtonGreenStyle}"
                                Padding="15,8" HorizontalAlignment="Left" Margin="0,20,0,0"
                                Click="btnSaveCompanyInfo_Click"/>
                    </StackPanel>
                </Border>

                <Border Background="#1AFFFFFF" CornerRadius="5" Padding="20" Margin="0,15,0,0">
                    <StackPanel>
                        <TextBlock Text="จัดการทีมผู้สร้าง (Team Members)" Foreground="White" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>

                        <Border Background="#2C2C2E" CornerRadius="5" Padding="10" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Top" Margin="0,0,15,0">
                                    <Border Width="80" Height="80" CornerRadius="40" Background="#444" BorderBrush="#666" BorderThickness="1">
                                        <Image x:Name="imgMemberPreview" Stretch="UniformToFill"/>
                                    </Border>
                                    <Button x:Name="btnBrowseMemberImage" Content="เลือกรูป" Margin="0,5,0,0" Padding="5" Click="btnBrowseMemberImage_Click"/>
                                    <TextBlock x:Name="txtMemberImagePath" Text="" Visibility="Collapsed"/>
                                    <TextBlock x:Name="txtEditingMemberId" Text="0" Visibility="Collapsed"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="ชื่อ-นามสกุล:" Foreground="#AAA" Margin="0,0,0,5"/>
                                    <TextBox x:Name="txtMemberName" Padding="5"/>
                                    <TextBlock Text="ตำแหน่ง (เช่น Developer):" Foreground="#AAA" Margin="0,10,0,5"/>
                                    <TextBox x:Name="txtMemberRole" Padding="5"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" VerticalAlignment="Bottom" Margin="10,0,0,0">
                                    <Button x:Name="btnAddMember" Content="เพิ่มทีม" Padding="10,8" Background="DodgerBlue" Foreground="White" Click="btnAddMember_Click"/>
                                    <Button x:Name="btnCancelEditMember" Content="ยกเลิก" Padding="10,8" Background="Gray" Foreground="White" Margin="0,5,0,0" Click="btnCancelEditMember_Click" Visibility="Collapsed"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <ListBox x:Name="TeamListBox" Background="Transparent" BorderThickness="0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#3A3A3C" CornerRadius="5" Padding="10" Margin="0,5,0,5" Width="600">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Border Width="50" Height="50" CornerRadius="25" Background="#555" Margin="0,0,10,0">
                                                <Image Source="{Binding ImagePreview}" Stretch="UniformToFill"/>
                                            </Border>

                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" Foreground="White" FontWeight="Bold" FontSize="16"/>
                                                <TextBlock Text="{Binding Role}" Foreground="#AAA" FontSize="12"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                                <Button Content="แก้ไข" Style="{StaticResource SecondaryActionButtonStyle}" Background="DarkGoldenrod" Foreground="White" Margin="5" Tag="{Binding}" Click="btnEditMember_Click"/>
                                                <Button Content="ลบ" Style="{StaticResource SecondaryActionButtonStyle}" Background="DarkRed" Foreground="White" Margin="5" Tag="{Binding MemberId}" Click="btnDeleteMember_Click"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

    </Grid>
</Page>